<UserControl x:Class="FileFlow.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FileFlow"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="800"
             Background="{DynamicResource BackgroundBrush}">
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <ObjectDataProvider x:Key="ThemeEnumValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:ThemeManager+Theme"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,30">
            <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,30"/>

            <TextBlock Text="Project Folder Path" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,15"/>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Current Path:" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding ProjectFolderPath, Mode=OneWay}" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="New Path:" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditableProjectFolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5" />
                    <Button Content="Save and Reload Projects" Command="{Binding SaveAndReloadCommand}" Style="{DynamicResource ModernButton}" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Appearance" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,30,0,15"/>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Select a theme:" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource ThemeEnumValues}}" SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Content Indexing Paths" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,30,0,15"/>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Add folder paths here to index the content of their files. For other paths, only filenames will be indexed." TextWrapping="Wrap" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,0,0,10"/>
                    <DockPanel>
                        <Button Content="Add Path" DockPanel.Dock="Right" Command="{Binding AddPathCommand}" Style="{DynamicResource ModernButton}" Margin="10,0,0,0"/>
                        <TextBox Text="{Binding NewPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalContentAlignment="Center"/>
                    </DockPanel>
                    <ListView ItemsSource="{Binding IndexedPaths}" Background="Transparent" BorderThickness="0" Margin="0,10,0,0" Foreground="{DynamicResource ForegroundBrush}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <Button Content="Remove" DockPanel.Dock="Right" Command="{Binding DataContext.RemovePathCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}" Style="{DynamicResource ModernButton}" Background="IndianRed" Padding="8,4" FontSize="10"/>
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                </DockPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <TextBlock Text="Search Index" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,30,0,15"/>
            <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Service Status:" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding ApiStatusMessage}" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,0,0,20"/>

                    <TextBlock Text="The search index is now managed by the server-side service. Manual controls are disabled." TextWrapping="Wrap" Foreground="{DynamicResource MutedForegroundBrush}" Margin="0,0,0,10"/>

                    <Button Content="Build Search Index" IsEnabled="False" Style="{DynamicResource ModernButton}" HorizontalAlignment="Left"/>

                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
